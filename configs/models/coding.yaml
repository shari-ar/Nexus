id: coding
name: Nexus Coding
capability: coding
provider: vllm
served_model_name: ${CODING_MODEL_NAME:-nexus-coding}
model_path: ${CODING_MODEL_PATH:-Qwen/Qwen2.5-Coder-0.5B-Instruct}
api_base_url: http://vllm:8000/v1
recommended_settings:
  temperature: 0.1
  top_p: 0.9
  max_tokens: 1536
operational_requirements:
  tensor_parallel_size: ${VLLM_TENSOR_PARALLEL_SIZE:-1}
  minimum_memory: 4 GB GPU VRAM or compatible CPU fallback environment
  startup_health_endpoint: /health
  models_endpoint: /v1/models
verification:
  command: ./scripts/models/test-vllm-prompt.sh "Write one sentence describing Nexus."
  expected_result: Terminal output includes a non-empty assistant response from the configured vLLM model.
