# Bootstrap Scripts

Use these scripts to prepare local development directories before starting the Docker development stack.

```bash
scripts/bootstrap/dev-env.sh
```

The script creates the `data/`, `memory/`, and `logs/` directory trees used by Nexus services in development mode.
