{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nexus.local/contracts/orchestration-request.schema.json",
  "title": "Nexus Orchestration Request",
  "type": "object",
  "required": ["request"],
  "properties": {
    "request": {
      "type": "string",
      "minLength": 1,
      "description": "The user-visible request that should be routed to CrewAI."
    },
    "source": {
      "type": "string",
      "default": "open-webui",
      "description": "The upstream interface or integration that submitted the request."
    },
    "request_id": {
      "type": "string",
      "description": "Correlation ID generated by the adapter or upstream caller."
    }
  },
  "additionalProperties": true
}
